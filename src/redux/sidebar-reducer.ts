
import {ActionType} from "./dialogs-reducer";
import {AppRootStateType, store} from "./store";

export const sidebarReducer = (state: AppRootStateType = store.getState(), action: ActionType) => {

    return state
}